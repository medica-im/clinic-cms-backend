[pytest]
DJANGO_SETTINGS_MODULE = backend.settings
asyncio_mode = auto
pythonpath = .
testpaths = tests
markers =
    integration: test that requires a real Neo4j container (deselect with -m "not integration")
